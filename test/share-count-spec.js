var assert = require('chai').assert;
var shareCount = require('../share-count');

describe('share-count', function() {

  describe('get', function() {

    it('should do something', function() {
      assert.ok(true);
    });

  });

});
